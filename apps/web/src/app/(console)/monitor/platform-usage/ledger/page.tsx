import { PlatformUsageLedgerContent } from '@/components/platform-event-usage/platform-usage-ledger-content';

export default function PlatformUsageLedgerPage() {
  return <PlatformUsageLedgerContent />;
}
