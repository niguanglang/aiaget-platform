export default function ApiReferencePlanPage() {
  return (
    <main className="mx-auto max-w-4xl px-6 py-10">
      <h1 className="text-2xl font-semibold">API Plan</h1>
      <p className="mt-3 text-sm leading-6 text-muted-foreground">
        OpenAPI-backed Control Plane routes and Runtime contracts will be introduced stage by
        stage. M00 keeps this page as a placeholder so the frontend route model is explicit.
      </p>
    </main>
  );
}

