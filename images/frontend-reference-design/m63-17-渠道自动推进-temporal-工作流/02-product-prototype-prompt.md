# Product Prototype / Wireframe Prompt

Create a low/mid-fidelity product prototype image for the `/channels` release automation workflow panel.

Show:
- Header: M63-17 badge, workflow mode badge, backend badge.
- Metrics: executor status, gate decision, today's runs, workflow backend.
- Detail grid: current batch, batch status, can run, next allowed time, workflow mode, execution backend.
- Last run details: run id, workflow id, backend, mode, batch id, gate decision, started/finished time.
- Event feed: workflow dispatched, local fallback, temporal dispatch failed, promoted/skipped.

Focus on information architecture and state mapping. Do not invent extra backend fields.
