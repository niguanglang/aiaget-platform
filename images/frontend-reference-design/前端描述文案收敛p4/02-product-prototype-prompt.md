# Product Prototype / Wireframe Prompt

中保真后台原型图，展示文案收敛后的页面规则：页面头部只保留返回按钮、状态标签、标题和核心操作；列表区只保留筛选、表格、分页和行内操作；空状态只显示短标题和可执行按钮；详情页只显示字段组和操作，不显示教学式说明；模块卡片只保留标题和入口。用中文标注，但文字短、直接、业务化。明确移除描述段落、副标题解释、占位式介绍、内部规划文案。
