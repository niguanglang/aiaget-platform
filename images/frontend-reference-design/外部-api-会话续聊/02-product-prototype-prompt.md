# Product Prototype / Wireframe Prompt

Create a product prototype / wireframe image for the `/api-reference` page section about external API conversation continuation.

Prototype requirements:
- Show sections:
  - endpoint matrix: create JSON, create SSE, continue JSON, continue SSE
  - main task flow
  - request fields and response fields
  - code examples for continue JSON and continue SSE
  - security checks and common errors
- Keep component boundaries obvious for `Card`, `Button`, `MetricCard`, `StatusBadge`, `SchemaCard`, `CodeCard`.
- Focus on information architecture and route documentation, not marketing.
- Chinese labels only.
